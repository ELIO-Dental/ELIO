# Architecture Decision Records

One file per non-trivial architectural decision, numbered sequentially
(`0001-...md`, `0002-...md`), using `0000-template.md` as the starting point.

The first real ADR expected here is the RLS-vs-query-wrapper decision from
`project-docs/MASTER_BUILD_GUIDE.md` Step 1.3 (multi-tenant row scoping) — not
written yet, since Step 1.3 hasn't happened. Add it there, not here.
